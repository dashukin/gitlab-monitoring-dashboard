/*
* Constants
* */

import keymirror from 'keymirror';

const mergeRequestsConstants = keymirror({
  MERGE_REQUESTS_FETCH: null,
  MERGE_REQUESTS_FETCH_SUCCESS: null,
  MERGE_REQUESTS_FETCH_ERROR: null,
});

export default mergeRequestsConstants;
